ARRAY_NAME="Greenplum Single Node in docker"
SEG_PREFIX="${GREENPLUM_SEG_PREFIX}"
DATABASE_NAME="${GREENPLUM_DATABASE_NAME}"
PORT_BASE=6000
MASTER_HOSTNAME="gpdb6-sne"
# MASTER_DIRECTORY="${GREENPLUM_DATA_DIRECTORY}/master"
MASTER_DIRECTORY="/gpmaster"
MASTER_PORT=5432
TRUSTED_SHELL=ssh
CHECK_POINT_SEGMENTS=8
ENCODING=UNICODE
# MACHINE_LIST_FILE="${GREENPLUM_DATA_DIRECTORY}/hostlist_singlenode"
# скорее всего в sh прибито гвоздями
MACHINE_LIST_FILE="${GREENPLUM_DATA_DIRECTORY}/hostfile_gpinitsystem"
# declare -a DATA_DIRECTORY=(${GREENPLUM_DATA_DIRECTORY}/00/primary ${GREENPLUM_DATA_DIRECTORY}/01/primary)
declare -a DATA_DIRECTORY=(/gpdata1 /gpdata2)
